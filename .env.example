# LocalStack (Ultimate plan auth token)
LOCALSTACK_AUTH_TOKEN=your-localstack-auth-token

# Sanity webhook HMAC secret — set this when configuring the Sanity webhook
SANITY_WEBHOOK_SECRET=your-sanity-webhook-secret

# GitHub PAT — needed for CodeBuild to clone the private repo
GITHUB_TOKEN=your-github-pat
